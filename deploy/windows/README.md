# deploy/windows

Populated in **M6**.

WiX MSI source, Windows Service registration assets, default config under `%PROGRAMDATA%\Conduit\`, and the `install.ps1` PowerShell installer. Authenticode signing is a release-pipeline concern (M12); the EV cert procurement is tracked as an open ADR.
